<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        a(href='/estudiantes') Atrás
        h1 #{estudiante.nombre}
        h2 Tiene #{estudiante.edad} años.